#!/bin/bash -e
# initialize Ghost

. /etc/default/inithooks

[ -e $INITHOOKS_CONF ] && . $INITHOOKS_CONF

$INITHOOKS_PATH/bin/ghost_inithook.py --address --uname --email --password
ghostdb=/opt/ghost/content/data/ghost.db
chown node:node $ghostdb
