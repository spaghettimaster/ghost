#!/bin/bash -e
# initialize Ghost

. /etc/default/inithooks

[ -e $INITHOOKS_CONF ] && . $INITHOOKS_CONF

$INITHOOKS_PATH/bin/ghost_inithook.py

#restart ghost to use newly defined URL
su -c "cd /opt/ghost/ && pm2 restart ghost" node
